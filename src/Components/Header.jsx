
const Header = ({titulo}) => {
    return ( 
        <nav className="nac-wrapper blue darker-3">
            <a href="#!" className="brand-logo center">{titulo}</a>
        </nav>
     );
}
 
export default Header;